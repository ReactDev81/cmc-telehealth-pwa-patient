
services:
  cmc-telehealth-pwa-patient:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL}
    container_name: cmc-telehealth-pwa-patient
    restart: unless-stopped

    ports:
      - "8000:8000"

    env_file:
      - .env
